version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "No build step required"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
